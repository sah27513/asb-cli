describe("Topic Utilites", () => {
  beforeEach(() => {
    // CreateTopic = jest.spyOn(shared, "createTopic");
  });

  test("It Returns the Topic Name without an error", async next => {
    // Setup the test
    // const sb = init();

    // Run the Test
    // const topic = createTopic(sb, "example");

    // Complete The test
    next();
  });
});
